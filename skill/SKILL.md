---
name: tend
description: Check in on tend and clear one thing. Use when the user runs /tend.
---

Run `tend draw` and show the picture as your response. Do not run any other
`tend` command yet.

If the user's next message names exactly one stalk number (1-7), run
`tend squish <N>` and show the result. Otherwise the check-in is over —
just continue with whatever they asked for.

Do not explain what the picture means.
